<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <%= link_to(root_path, class: "navbar-brand") do %>
      <% image_pack_tag "Logo_PKP.webp" %>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <% if current_user.admin %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <%= t('.admin') %>
              </a>
              <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <%= link_to t('.users'), admin_users_path, class: "dropdown-item" %>
                <%= link_to t('.courses'), admin_courses_path, class: "dropdown-item" %>
                <%= link_to t('.subscriptions'), admin_subscriptions_path, class: "dropdown-item" %>
              </div>
            </li>
          <% end %>
          <%= link_to t('.my_account'), edit_dashboard_profile_path, class: "nav-link" %>
        <% end %>
      </ul>
      <div class="menu-container">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <% if user_signed_in? %>
              <%= link_to t('defaults.logout'), destroy_user_session_path, method: :delete, class: "nav-link" %>
            <% else %>
              <%= link_to t('devise.shared.links.sign_in'), new_user_session_path, class: "nav-link" %>
            <% end %>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>
