<div class="row">
  <div class="col-md-6 offset-md-3">
    <h1><%= t('.title') %></h1>

    <%= render 'form', subscription: @subscription, url: admin_subscription_path(@subscription.id) %>

    <%= link_to t('defaults.back'), admin_subscriptions_path %>
  </div>
</div>
