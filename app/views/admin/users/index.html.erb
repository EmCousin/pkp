<h1 class="page-title"><%= t(".title") %></h1>
<hr />

<div class="row">
  <div class="col-12">
    <div class="row">
      <div class="col-md">
        <%= form_with(url: admin_users_path, local: true, method: "get") do %>
          <div class="input-group mb-3">
            <%= text_field_tag(:q, @search, class: "form-control mr-sm-2", placeholder: "Search") %>
            <div class="input-group-append">
              <%= submit_tag("Search", class: "btn btn-success") %>
            </div>
          </div>
        <% end %>
      </div>
      <div class="col-md">
        <%= link_to t('defaults.new', resource: User.model_name.human.downcase), new_admin_user_path, class: "btn btn-primary float-right" %>
      </div>
    </div>
    <table class="table mt-5">
      <thead>
        <tr>
          <th><%= User.human_attribute_name(:full_name) %></th>
          <th class="d-none d-md-block"><%= User.human_attribute_name(:birthdate) %></th>
          <th><%= User.human_attribute_name(:phone_number) %></th>
          <th class="d-none d-sm-block">Action</th>
        </tr>
      </thead>

      <tbody>
        <% if @users.empty? %>
          <tr>
            <td colspan="9" class="text-center">
              <%= link_to t('.add_user'), new_admin_user_path, class: "btn btn-primary" %>
            </td>
          </tr>
        <% else %>
          <% @users.each do |user| %>
            <tr>
              <td><%= image_tag url_for(user.avatar), width: "30" %> <%= link_to user.full_name.capitalize.titleize, admin_user_path(user.id) %></td>
              <td class="d-none d-md-block"><%= user.birthdate %></td>
              <td><%= link_to user.phone_number, "tel:#{user.phone_number}" %></td>
              <td class="d-none d-sm-block">
                <%= link_to t('defaults.edit'), edit_admin_user_path(user.id), class: "btn btn-outline-warning btn-sm" %>
                <%= link_to t('defaults.destroy'), admin_user_path(user.id), method: :delete, data: { confirm: t('defaults.are_you_sure') }, class: "btn btn-outline-danger btn-sm" %>
              </td>
            </tr>
          <% end %>
        <% end %>
      </tbody>
    </table>
    <%= paginate @users %>
  </div>
</div>
