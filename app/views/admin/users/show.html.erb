<p>
  <strong><%= User.human_attribute_name(:first_name) %> :</strong>
  <%= @user.first_name %>
</p>

<p>
  <strong><%= User.human_attribute_name(:last_name) %> :</strong>
  <%= @user.last_name %>
</p>

<p>
  <strong><%= User.human_attribute_name(:phone_number) %> :</strong>
  <%= @user.phone_number %>
</p>

<p>
  <strong><%= User.human_attribute_name(:avatar) %> :</strong>
  <%= @user.avatar %>
</p>

<br/>

<%= link_to t('defaults.edit'), edit_admin_user_path(@user.id) %>
<%= link_to t('defaults.destroy'), admin_user_path(@user.id), method: :delete, data: { confirm: 'Are you sure?' } %>
